<% if @ropas.empty? %>
  <h3>En estos momentos no disponemos de productos de este tipo, lo sentimos</h3>
<% end %>

<%= render :partial => "ropa" %>

<%= will_paginate @ropas, :renderer => BootstrapPagination::Rails %>