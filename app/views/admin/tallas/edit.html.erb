<div class="row">
	<div class="col-md-4">
		<%= form_tag admin_talla_path(@talla), :method => 'put' do %>
		  <%= render :partial => 'form' %>
		  <%= submit_tag 'Actualizar talla', :class => 'btn btn-primary' %>
		<% end %>
	</div>
</div>

<br>
<%= link_to 'Mostrar', admin_talla_path(@talla), :class => 'btn btn-success' %>
<%= link_to 'Volver', admin_tallas_path, :class => 'btn btn-default btn-sm' %>