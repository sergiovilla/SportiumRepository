<!DOCTYPE html>
<html>
<head>
  <title><%= @page_title || 'Sportium' %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
  	<div class="container">
		<div class="masthead">
	    <h3 class="text-muted">Sportium&trade;</h3>
	      <ul class="nav nav-justified">
	        <li id="inicio"><%= link_to 'Inicio', root_path %></li>
	        <li id="marcas"><a href="#">Marcas</a></li>
	        <li id="camisetas"><a href="#">Camisetas</a></li>
	        <li id="calzonas"><a href="#">Calzonas</a></li>
	        <li id="equip"><a href="#">Equipaciones</a></li>
	        <li id="zapatillas"><a href="#">Zapatillas</a></li>
	      </ul>
	    </div>
    </div>
  </header>
	
	<div class="container">
	<h2><%= @page_title %></h2>

	<% flash.each do |nivel, msg| %>
	<div class="<%= flash_class(nivel) %>">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
    	<% if nivel.eql? :error %>
    		<h3><%= pluralize(msg.count, "error", "errores") %></h3>
    		<ul><% msg.each do |razon| %>
        	<li><%= razon %></li>
      	<% end %>
    		</ul>
    	<% else %>
    		<%= msg %>
    	<% end %>
    </div>
  <% end %>

  	<%= yield %>

		<div class="footer">
			&copy; 2013-2014 Computer Heat 2.0
		</div>
	</div> <!-- Container principal -->
</body>
</html>
