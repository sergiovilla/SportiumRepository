<%= link_to item.ropa.nombre, catalogo_show_path(item.ropa.id) %>

<%= pluralize item.cantidad, item.ropa.tipo %>, <%= sprintf "%0.2f €", item.precio * item.cantidad %>

(<%= link_to '<b>-</b>'.html_safe, :controller => 'cart', :action => 'remove', :id => item.ropa %>)